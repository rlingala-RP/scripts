pipeline{
    agent {
        label 'slave'
    }
    stages{
        stage('checkout') {
            steps {
                git credentialsId: 'jenkins', url: 'git@github.com:rlingala-RP/Gowebapi.git'
            }

        }
    }
}